<h3>Sup babes</h3>

<%= link_to "New Product", new_product_path %>
<hr>

<ul>
  <% @products.each do |product| %>
    <p><%= link_to product.name, product_path(product) %>, Reviews: <%= product.reviews.length %>, Price $<%= product.cost %>, Average Rating: <%= product.average_rating %> </p>
    <hr>
  <% end %>
</ul>
